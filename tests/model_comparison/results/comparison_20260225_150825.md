# Model Comparison Report: `run-pipeline` skill
Generated: 20260225_150825

## Summary

| Model | Overall Score | Recommendation |
|-------|--------------|----------------|
| `sonnet` | 73.8 | **FAIL** — Critical failures on mandatory criteria: ['summary_read'] |
| `haiku` | 73.8 | **FAIL** — Critical failures on mandatory criteria: ['summary_read'] |

---

## Model: `sonnet`

**Overall score**: 73.8 / 100
**Recommendation**: **FAIL**
**Reason**: Critical failures on mandatory criteria: ['summary_read']

**Critical failures**: summary_read

### Scenario: `happy_path`

**ERROR**: claude CLI not found — ensure it is installed and on PATH

### Scenario: `nonexistent_neighborhood`

**ERROR**: claude CLI not found — ensure it is installed and on PATH

### Scenario: `list_neighborhoods`

**ERROR**: claude CLI not found — ensure it is installed and on PATH

### Scenario: `empty_args`

**ERROR**: claude CLI not found — ensure it is installed and on PATH

### Scenario: `lowercase_neighborhood`

**ERROR**: claude CLI not found — ensure it is installed and on PATH

---

## Model: `haiku`

**Overall score**: 73.8 / 100
**Recommendation**: **FAIL**
**Reason**: Critical failures on mandatory criteria: ['summary_read']

**Critical failures**: summary_read

### Scenario: `happy_path`

**ERROR**: claude CLI not found — ensure it is installed and on PATH

### Scenario: `nonexistent_neighborhood`

**ERROR**: claude CLI not found — ensure it is installed and on PATH

### Scenario: `list_neighborhoods`

**ERROR**: claude CLI not found — ensure it is installed and on PATH

### Scenario: `empty_args`

**ERROR**: claude CLI not found — ensure it is installed and on PATH

### Scenario: `lowercase_neighborhood`

**ERROR**: claude CLI not found — ensure it is installed and on PATH

---

## Side-by-Side: Happy Path Final Text

### `sonnet`
~~~

~~~

### `haiku`
~~~

~~~
